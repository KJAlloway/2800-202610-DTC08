# Kept Prototypes

These files contain ideas or data worth preserving.

They are not used by the clean app because they were created before the backend/provider architecture was defined. Keep them available for comparison, fixture design, and teammate context.
