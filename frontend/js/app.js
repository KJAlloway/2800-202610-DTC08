/**
 * Frontend entry point.
 *
 * Keep this file small: initialize page-level behavior and delegate work to
 * focused modules.
 */

import { initSearchForm } from './ui/searchForm.js';

initSearchForm();
